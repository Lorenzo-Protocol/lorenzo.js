/**
 * EIP712TxGenerator is a client that can be used to sign EIP712 payloads and generate signed tx bytes.
 *
 * It doesn't care about how the tx bytes are broadcast to the network but only focuses on generating
 * the signed tx bytes. For broadcasting the tx bytes, user can use the `StargateClient`.
 *
 */
export class EIP712TxGenerator {

}